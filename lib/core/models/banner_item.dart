/// Static banner/hero-carousel model used on the home screen (Model layer).
class BannerItem {
  final String image;
  final String title;
  final String subtitle;

  const BannerItem({
    required this.image,
    required this.title,
    required this.subtitle,
  });
}
